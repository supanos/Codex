# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=suponos
# Prisma/Node için standart URL (Docker'da hizmet adı: db)
DATABASE_URL=postgresql://postgres:password@db:5432/suponos?schema=public

# Auth
JWT_SECRET=please-change-me-to-a-long-random-string

# SMTP (opsiyonel — iletişim formu/e-posta gönderimi için)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_EMAIL=
